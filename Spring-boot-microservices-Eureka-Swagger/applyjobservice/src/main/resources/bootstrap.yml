spring:
   application:
      name: applyjobservice
   profiles:
      active: dev
   cloud:
      config:
         uri: http://configurations-server:8282
eureka:
   client:
      service-url:
         defaultZone: http://eureka-service-registry:8761/eureka/
Spring:
 zipkin:
  enabled: true
  baseurl: http://zipkin-server:9411
